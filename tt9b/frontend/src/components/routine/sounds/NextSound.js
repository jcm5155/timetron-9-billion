import React, { Component } from "react";
import Sound from "react-sound";

// This component will eventually be used to play sounds when a time segment/routine elapses.
class NextSound extends Component {
  render() {
    return <Sound {...props} />;
  }
}
